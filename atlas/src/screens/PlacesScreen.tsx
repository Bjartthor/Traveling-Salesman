import { EmptyState } from '@/components/layout/EmptyState'

export function PlacesScreen() {
  return (
    <EmptyState
      title="Places"
      description="Search for a country, region or city to mark it as wishlist, transit, visited or lived — that feature lands in a later build."
    />
  )
}
