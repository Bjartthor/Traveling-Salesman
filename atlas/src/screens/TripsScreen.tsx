import { EmptyState } from '@/components/layout/EmptyState'

export function TripsScreen() {
  return (
    <EmptyState
      title="Trips"
      description="Group your visits into trips here, and closed trips will print as a passport-style stamp once that feature is built."
    />
  )
}
