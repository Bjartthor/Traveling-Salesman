# Copy to .env.local and fill in your own Google OAuth Web-application client ID.
# See docs/OPERATIONS.md (written in the deploy phase) and plan §9 for how to
# create it. Not a secret — it ships in the client bundle by design.
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
